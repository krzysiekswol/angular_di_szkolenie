import { Routes } from '@angular/router';
import { TaskOneParentComponent } from './task_1/components/task-one-parent/task-one-parent.component';

export const routes: Routes = [
  { path: 'taskOne', component: TaskOneParentComponent }
];
